<p>All Hogwarts students:</p>

<% @students.each {|student| %>

  <p>First Name: <%= student.first_name() %></p>
  <p>Last Name: <%= student.last_name() %></p>
  <p>House: <%= student.house() %></p>
  <p>Age: <%= student.age() %></p>
  
<% } %>
